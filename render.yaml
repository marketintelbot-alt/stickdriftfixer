services:
  - type: web
    name: stickdrift-web
    runtime: static
    buildCommand: echo "static"
    staticPublishPath: web
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
